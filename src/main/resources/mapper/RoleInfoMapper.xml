<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.aurifolia.cloud.ginkgo.user.repository.RoleInfoMapper">

    <resultMap id="BaseResultMap" type="org.aurifolia.cloud.ginkgo.user.entity.RoleInfo">
        <id column="id" property="id"/>
        <result column="role_name" property="roleName"/>
        <result column="role_desc" property="roleDesc"/>
        <result column="deleted" property="deleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, role_name, role_desc, deleted, create_time, update_time
    </sql>

    <insert id="insert" parameterType="org.aurifolia.cloud.ginkgo.user.entity.RoleInfo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO role_info (role_name, role_desc, deleted, create_time, update_time)
        VALUES (#{roleName}, #{roleDesc}, #{deleted}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateById" parameterType="org.aurifolia.cloud.ginkgo.user.entity.RoleInfo">
        UPDATE role_info
        <set>
            <if test="roleName != null and roleName != ''">
                role_name = #{roleName},
            </if>
            <if test="roleDesc != null">
                role_desc = #{roleDesc},
            </if>
            <if test="deleted != null">
                deleted = #{deleted},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role_info
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectByRoleName" parameterType="string" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role_info
        WHERE role_name = #{roleName} AND deleted = 0
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM role_info
        WHERE deleted = 0
    </select>

    <update id="deleteById" parameterType="long">
        UPDATE role_info
        SET deleted = 1
        WHERE id = #{id}
    </update>

</mapper>